@using Microsoft.AspNetCore.Http

<div class="header-wrapper container">
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a asp-controller="Home" asp-action="Index" class="logo-text">DineGO</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll"
        aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarScroll">
        <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">

          <li class="nav-item">
            <a class="nav-link" asp-controller="Restaurant" asp-action="Index">Nhà hàng gần bạn</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Nhà hàng uy tín</a>
          </li>
          </li class="nav-item">
          <a class="nav-link" asp-controller="Blog" asp-action="Index">Tin tức và blog</a>
          </li>
          </li class="nav-item">
          <a class="nav-link" href="#">Báo cáo</a>
          </li>
        </ul>

        @{
          var cusId = Context.Session.GetInt32("cus_id");
        }
        @if (cusId.HasValue)
        {
          <p>Welcome, @cusId</p>
        }
        else
        {
          <p>Welcome, Guest</p>
        }

        <a class="nav-link login-section" asp-controller="Auth" asp-action="Login">
          <i class="fas fa-user"></i> Tài khoản
        </a>
        <a class="nav-link login-section" asp-controller="Auth" asp-action="Logout">
          <i class="fas fa-user"></i> Đăng xuất
        </a>

      </div>
    </div>
  </nav>

</div>